interface:
  display_name: "Contract CLI Shared"
  short_description: "Choose the right contract-cli open platform module and follow shared command rules"
  default_prompt: "Use $contract-cli-shared to choose between contract, mdm vendor, mdm legal, mdm fields, and api-call skills, then follow the shared `--as user`, `--input-file`, and output conventions."

policy:
  allow_implicit_invocation: true
